run "npm install" in the terminal to install the project and to run the test with "npx playwright test --headed"

this project was built as a way to study project structure. fixtures, data files, importing them, exporting them, etc etc. preventing security risks with .env file too.